@inherits LayoutComponentBase
@inject AuthenticationService service
@inject NavigationManager navManager
<div class="page">
    <AuthorizeView>
        <Authorized>
            <div class="sidebar  w-auto" style="background-color:#061726">
                <NavMenu OnLogout="Logout" />
            </div>
        </Authorized>
    </AuthorizeView>

    <main>
        <AuthorizeView>
            <NotAuthorized>
                <Pobytne.Client.Pages.Login />
            </NotAuthorized>
            <Authorized>

                <article class="content px-4">
                    @Body
                </article>
            </Authorized>
        </AuthorizeView>
    </main>
</div>

@code {
    [CascadingParameter]
    private Task<AuthenticationState> authenticationState { get; set; } = default!;
    private async Task Logout()
    {
        await service.Logout();
    }
    // private async Task RefreshTokenHandler()
    // {
    //     var authState = await authenticationState;
    //     if (authState.User.Identity is not null && authState.User.Identity.IsAuthenticated)
    //         await service.CheckTokenValidity();
    //     await Task.Delay(1* 10 * 1000);
    //     await RefreshTokenHandler();
    // }
}