@page "/{idModule}/CashRegister"
@attribute [Authorize]

<ModuleNav idModule="@idModule" PageTitle="Pokladna"></ModuleNav>

@code {
    [EditorRequired]
    [Parameter]
    public string idModule { get; set; } = "-1";
}
