@page "/{idModule}/RecordReports"
@attribute [Authorize]

<ModuleNav idModule="@idModule" PageTitle="Evidence zásob na skladě"></ModuleNav>


@code {
    [EditorRequired]
    [Parameter]
    public string idModule { get; set; } = "-1";
}